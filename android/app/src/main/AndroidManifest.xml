<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".WormholeApp"
        android:allowBackup="false"
        android:icon="@drawable/ic_wormhole"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Sentry: auto-init via ContentProvider before Application.onCreate() -->
        <meta-data android:name="io.sentry.dsn"
            android:value="https://d3c95e3fc6f8be0d32b42244de016180@o4504272346480640.ingest.us.sentry.io/4511254231973888" />
        <meta-data android:name="io.sentry.traces-sample-rate" android:value="0.2" />
        <meta-data android:name="io.sentry.auto-init" android:value="true" />

        <!-- Minimal main activity: shown only for initial Google sign-in -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Share target: appears in the Android share sheet for any file type.
            The activity finishes quickly after starting the transfer.
        -->
        <activity
            android:name=".ShareActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <!-- Also accept image/* specifically so photo apps see us -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!--
            Foreground service that performs the actual wormhole receive.
            Started when the user taps the "Принять" action on the notification.
        -->
        <service
            android:name=".ReceiveService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- FCM message handler -->
        <service
            android:name=".FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Handles "Decline" tap on incoming-file notification -->
        <receiver
            android:name=".DeclineReceiver"
            android:exported="false" />

    </application>
</manifest>
